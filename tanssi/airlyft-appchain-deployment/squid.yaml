manifestVersion: subsquid.io/v0.1
name: airlyft
version: 1
description: Substrate squid template
build: 
deploy:
  addons:
    postgres: 
  processor:
    - name: dancebox-processor
      cmd: ['sqd', 'process:dancebox']
    - name: relay-processor
      cmd: ['sqd', 'process:relay']
  api:
    cmd:
      - sqd
      - serve:prod
